"Regular Boot" "initrd=\initramfs-%v.img {{ 'cryptdevice=UUID=' + refind_cryptdevice_uuid + ':' + refind_cryptdevice_container if refind_cryptdevice_uuid and refind_cryptdevice_container else '' }} {{ 'root=' + refind_root if refind_root else '' }} {{ 'resume=' + refind_resume if refind_resume else '' }} {{ refind_additional_kernel_params }}"
"Fallback Boot" "initrd=\initramfs-%v-fallback.img {{ 'cryptdevice=UUID=' + refind_cryptdevice_uuid + ':' + refind_cryptdevice_container if refind_cryptdevice_uuid and refind_cryptdevice_container else '' }} {{ 'root=' + refind_root if refind_root else '' }} {{ 'resume=' + refind_resume if refind_resume else '' }} {{ refind_additional_kernel_params }}"
