HOOKS=(base udev keyboard autodetect {{ 'microcode' if mkinitcpio_microcode_hook else '' }} modconf {{ 'kms' if mkinitcpio_kms_hook else '' }} {{ 'keymap' if mkinitcpio_keymap_hook else '' }} {{ 'consolefont' if mkinitcpio_consolefont_hook else '' }} block {{ 'netconf tinyssh encryptssh' if mkinitcpio_encrypt_ssh_hook else ('encrypt' if mkinitcpio_encrypt_hook else '') }} {{ 'lvm2' if mkinitcpio_lvm2_hook else '' }} filesystems {{ 'resume' if mkinitcpio_resume_hook else '' }} fsck)
