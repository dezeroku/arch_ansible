---
- name: Ensure builds/st folder exists
  file:
    path: /home/{{ builder.name }}/builds/st
    state: directory
    recurse: yes

- name: Copy config
  copy:
    src: config.h
    dest: /home/{{ builder.name }}/builds/st

- name: Copy PKGBUILD
  copy:
    src: PKGBUILD
    dest: /home/{{ builder.name }}/builds/st

- name: Build and install
  command: chdir=/home/{{ builder.name }}/builds/st makepkg -si --noconfirm
